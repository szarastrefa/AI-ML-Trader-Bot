# Core Flask and API
Flask==2.3.3
Flask-CORS==4.0.0
Flask-SocketIO==5.3.6
Flask-JWT-Extended==4.5.3
Flask-Limiter==3.5.0
Flask-SQLAlchemy==3.0.5
Werkzeug==2.3.7

# Database and ORM
SQLAlchemy==2.0.23
psycopg2-binary==2.9.7
redis==5.0.1

# Task queue
celery==5.3.4
celery[redis]==5.3.4

# Data processing (compatible versions)
numpy>=1.21.0,<1.25.0
pandas>=1.5.0,<2.1.0
scipy>=1.9.0,<1.12.0

# Technical Analysis (using 'ta' instead of problematic TA-Lib)
ta==0.10.2

# CCXT for crypto exchanges
ccxt==4.1.28

# ML libraries (optional, with compatible versions)
scikit-learn>=1.3.0,<1.4.0
joblib==1.3.2

# Networking and HTTP
requests==2.31.0
websocket-client==1.6.4
websockets==12.0
aiohttp==3.9.1

# Configuration and environment
python-dotenv==1.0.0
PyYAML==6.0.1
click==8.1.7

# Logging and monitoring
loguru==0.7.2
coloredlogs==15.0.1

# Financial data (optional)
yfinance==0.2.28

# Security (FIXED VERSION)
cryptography>=41.0.0,<47.0.0
PyJWT==2.8.0

# Development and testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Utilities
python-dateutil==2.8.2
pytz==2023.3
tqdm==4.66.1
psutil==5.9.6

# JSON and data serialization
orjson==3.9.10

# Additional Flask extensions
flasgger==0.9.7.1

# WebSocket support
python-socketio==5.10.0

# Development tools (optional)
colorama==0.4.6
click-log==0.4.0

# Additional trading libraries (optional)
# MetaTrader5==5.0.45  # Only install if MT5 is needed
# ib_insync==0.9.86     # Only install if IB is needed  
# quickfix==1.15.1      # Only install if FIX protocol is needed